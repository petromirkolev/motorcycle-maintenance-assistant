services:
  postgres:
    environment:
      POSTGRES_DB: motocare_test

  api:
    environment:
      DATABASE_URL: postgres://postgres:postgres@postgres:5432/motocare_test
      PORT: 3001
      NODE_ENV: test
